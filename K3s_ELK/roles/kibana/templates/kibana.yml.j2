#
# ** THIS IS AN AUTO-GENERATED FILE. DO NOT EDIT IT MANUALLY. **
#
# Kibana configuration file.
#

# The port to serve Kibana from.
server.port: 5601

# Specifies the address to which the Kibana server will bind.
# Set to 0.0.0.0 to make it accessible from other machines.
server.host: "0.0.0.0"

# The URLs of the Elasticsearch instances to use for all your queries.
# This will be dynamically populated with all hosts from the 'elk_servers' group.
elasticsearch.hosts: [{% for host in groups['elk_servers'] %}"http://{{ hostvars[host]['ansible_host'] | default(host) }}:9200"{% if not loop.last %}, {% endif %}{% endfor %}]

# Kibana is served by a back end server. This setting specifies the public URL at
# which users can access Kibana.
# server.publicBaseUrl: ""

# Enables you to specify a path to mount Kibana at if you are running behind a proxy.
# server.basePath: ""

# The maximum payload size in bytes for incoming server requests.
# server.maxPayloadBytes: 1048576

# The Kibana server's name.
# server.name: "your-hostname"

# The paths to your custom plugins.
# plugins.paths: []

# Enables SSL and paths to the PEM-format SSL certificate and SSL key files, respectively.
# These settings enable SSL for outgoing requests from the Kibana server to the browser.
# server.ssl.enabled: false
# server.ssl.certificate: /path/to/your/server.crt
# server.ssl.key: /path/to/your/server.key
